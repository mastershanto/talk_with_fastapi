"""Domain modules (feature-first).

This package is the start of a gradual move toward a modular monolith with
hexagonal/clean-architecture principles:

- `domains/*` holds business-oriented modules (use-cases/services + ports).
- Framework/DB concerns live in `infrastructure/*` and routers.
"""
